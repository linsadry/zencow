import React from 'react';
import ReactDOM from 'react-dom/client';
import ZenCow from './src/ZenCow.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ZenCow />
  </React.StrictMode>
);
